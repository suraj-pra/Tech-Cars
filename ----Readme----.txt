Folder -> IP-XII is the main project to be opened in Java Netbeans.

Folders -> Download, Game, New Folder, Path, temp - are to be pasted in "D" drive

Files -> raj (ip), sql (project), Table (project), car (car) - are sql database files which has to copied in respective databases

Note -- car (car) is a .sql file and has to be backuped as usual in database named "car"


	raj (ip) - in database named "ip"
	sql (project), Table (project) - in database named "project"
	car (car) - in database named "car"


For sql file first create database as named above and then paste the whole text file in the respective database

ALERT!! -- Donot change any Folder's name otherwise the project wouldn't be successful